// Production environment configuration
export const environment = {
  production: true,
  // Backend API URL - Configure with environment variable or backend address
  // This will be replaced at build time or runtime based on deployment
  apiUrl: 'https://your-production-api-url.com',
  debug: false
};
