import{TestBed}from'@angular/core/testing';import{ActivatedRoute,convertToParamMap,Router}from'@angular/router';import{of,throwError}from'rxjs';import{AuditApi}from'../data-access/audit.api';import{AuditPageComponent}from'./audit-page.component';
describe('AuditPageComponent',()=>{it('retains the list when sanitized detail fails',()=>{const item:any={id:'507f1f77bcf86cd799439011',timestamp:'2026-09-01T00:00:00Z',level:'ERROR',message:'safe',eventType:'book.updated',metadata:{}};const api=jasmine.createSpyObj('AuditApi',['search','detail']);api.search.and.returnValue(of({items:[item],page:1,pageSize:25,totalItems:1}));api.detail.and.returnValue(throwError(()=>new Error('offline')));const params=convertToParamMap({eventType:'book.updated'});TestBed.configureTestingModule({imports:[AuditPageComponent],providers:[{provide:AuditApi,useValue:api},{provide:ActivatedRoute,useValue:{snapshot:{queryParamMap:params},queryParamMap:of(params)}},{provide:Router,useValue:jasmine.createSpyObj('Router',['navigate'])}]});const fixture=TestBed.createComponent(AuditPageComponent);fixture.detectChanges();fixture.componentInstance.open(item,{currentTarget:document.createElement('button')} as unknown as Event);fixture.detectChanges();expect(fixture.componentInstance.page()?.totalItems).toBe(1);expect(fixture.componentInstance.detailError()).toContain('detalle');expect(fixture.nativeElement.textContent).toContain('book.updated');});});
