import { ChangeDetectionStrategy, Component, EventEmitter, Input, OnChanges, Output } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { DashboardPeriodQuery } from './dashboard-period';
@Component({selector:'app-dashboard-period-controls',standalone:true,imports:[FormsModule],template:`<form class="period" (ngSubmit)="apply()"><label>Desde<input type="date" name="from" [(ngModel)]="draft.from" required></label><label>Hasta<input type="date" name="to" [(ngModel)]="draft.to" required></label><label>Zona IANA<input name="timezone" [(ngModel)]="draft.timezone" required aria-describedby="timezone-help"><small id="timezone-help">Los días se interpretan en esta zona.</small></label><button type="submit">Aplicar periodo</button><button type="button" (click)="refresh.emit()" [disabled]="busy">{{busy?'Actualizando…':'Actualizar'}}</button></form>`,styles:[`.period{display:flex;flex-wrap:wrap;align-items:flex-end;gap:.75rem;padding:1rem;border:1px solid var(--color-border);border-radius:var(--radius-card);background:var(--color-surface)}label{display:flex;min-width:10rem;flex:1;flex-direction:column;gap:.3rem;color:var(--color-text-muted);font-size:var(--text-xs);font-weight:800}input{min-height:2.75rem;padding:.5rem .7rem;border:1px solid var(--color-border-strong);border-radius:var(--radius-sm);background:white;font:inherit}small{font-weight:400}button{min-height:2.75rem;padding:.55rem 1rem;border:1px solid var(--color-primary);border-radius:var(--radius-pill);color:var(--color-primary);background:white;font-weight:800}button[type=submit]{color:white;background:var(--color-primary)}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class DashboardPeriodControlsComponent implements OnChanges { @Input({required:true}) period:DashboardPeriodQuery={from:'',to:'',timezone:'UTC'};@Input()busy=false;@Output()periodChange=new EventEmitter<DashboardPeriodQuery>();@Output()refresh=new EventEmitter<void>();draft={...this.period};ngOnChanges(){this.draft={...this.period};}apply(){if(this.draft.from&&this.draft.to&&this.draft.timezone)this.periodChange.emit({...this.draft});} }
